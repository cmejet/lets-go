{
  "name": "lets-go",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ cmejet/lets-go",
    "create-local": "graph create --node http://localhost:8020/ cmejet/lets-go",
    "remove-local": "graph remove --node http://localhost:8020/ cmejet/lets-go",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 cmejet/lets-go"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
